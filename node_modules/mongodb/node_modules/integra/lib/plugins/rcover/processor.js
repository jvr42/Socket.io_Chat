var Processor = function(options) {

	/**
	 * Merge all the coverage information into unified actionable item
	 */
	this.process = function(coverages) {
		var finalState = {};

		for(var i = 0; i < coverages.length; i++) {
			var coverage = coverages[i];
		}
	}
}