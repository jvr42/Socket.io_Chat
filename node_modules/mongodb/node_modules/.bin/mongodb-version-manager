#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongodb-version-manager/bin/mongodb-version-manager.js" "$@"
  ret=$?
else 
  node  "$basedir/../mongodb-version-manager/bin/mongodb-version-manager.js" "$@"
  ret=$?
fi
exit $ret
